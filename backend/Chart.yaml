apiVersion: v2
name: taskapp-backend
version: 0.1.0
description: A Helm chart for the TaskApp backend

type: application

dependencies:
  - name: taskapp-common
    version: "0.1.0"
    repository: "file://../common"